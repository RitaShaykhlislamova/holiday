import React from 'react';
import './gray-blockHouse.css';


const GrayBlock = ({ children }) => {
    return (
        <div className="gray-blockHouse">{children}</div>
    );
};

export default GrayBlock;